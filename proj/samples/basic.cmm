
int main() {
    consolePrintLnStr("Hello world from C minus minus");

    const int i = 25;
    consolePrintStr("Printing number 25: ");
    consolePrintLnNum(i);

    bool b = true;
    if (!(true) || !false ||b) {
        consolePrintLnStr("TRUE!!!");
    } else {
        consolePrintLnStr("FALSE!!!");
    }

    int num = 10;

    string lit = "a string literal";

    return num + 5;
}
